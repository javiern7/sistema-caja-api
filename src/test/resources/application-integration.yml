spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/sistema_caja
    username: cajaapi
    password: cajaapi
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

app:
  security:
    seed:
      enabled: true
